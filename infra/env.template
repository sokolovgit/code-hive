# Infrastructure Environment Variables
# Copy this file to .env and customize for your environment
# Or run: make setup

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=codehive
POSTGRES_PORT=5432

# Redis Configuration
REDIS_PASSWORD=redis
REDIS_PORT=6379

# pgAdmin Configuration (optional, for development)
PGADMIN_EMAIL=admin@codehive.local
PGADMIN_PASSWORD=admin

# Database Connection Strings (service-specific)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# Users Service
USERS_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/users-service
# Auth Service
AUTH_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/auth-service
# Payment Service
PAYMENT_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/payment-service

# Default DATABASE_URL (for backward compatibility, can be overridden per service)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/users-service

# Redis Connection String (used by services)
# Format: redis://:[password]@[host]:[port]
REDIS_URL=redis://:redis@localhost:6379

# OpenTelemetry Configuration
# OTLP endpoint for traces and metrics (OpenTelemetry Collector)
# Default: http://localhost:4317 (gRPC) or http://localhost:4318 (HTTP)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
